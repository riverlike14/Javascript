x == 0 && y == 0;

var o = { x: 1 };
var p = null;
o && o.x;
p && p.x;

if (a == b) stop();
(a == b) && stop();

var max = max_width || preferences.max_width || 500;

function copy(o, p) {
	p = p || {?};
}

!(p && q) === !p || !q;
!(p || q) === !p && !p;
