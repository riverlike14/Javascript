{
	x = Math.PI;
	cx = Math.cos(x);
	console.log("cos(pi) = " + cx);
}

;

for (i = 0; i < a.length(); a[i++] = 0) ;

if ((a == 0) || (b == 0));
	o = null;

for (int i = 0; i < a.length; a[i++] = 0) /* Empty */ ;
